

public interface interfaceVsAbsClass {
    /*
     * only abstract method, no implementation
     * variable type static and final
     * by default public
     * multiple interface can be inherit by a class 
     */
    /*
     * Comparator ekta interface
     * > compare ekta method
     * >we can overload this method 
     */
    int number = 0;
    String s = "my code";

    void doYour();
    
}
